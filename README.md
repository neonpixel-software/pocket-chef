# pocket-chef

- `app/` — the SwiftUI client (macOS, iPadOS, iOS)
- `api/` — the .NET density API (coming in Phase 8, see `PLAN.md`)

See `PLAN.md` for architecture and the phase-by-phase implementation plan.
